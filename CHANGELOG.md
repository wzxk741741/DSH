# 更新日志

## 0.3.0（当前）

- 修复：编辑按钮点开无反应（`EditorCard` 被当作普通函数调用，其 hooks 并入父组件序列导致
  React "Rendered fewer hooks than expected" 整树卸载；现经 `h()` 挂载，并加回归测试防线）
- 导入体验：导入结果直接显示在面板内（成功/失败原因/支持格式），错误提示延长，新增「填入示例」
- 导入兼容：支持 `system_prompt` / `prompt` 字段别名；`config.temperature / top_p / max_tokens`
  自动映射为性格参数；未提供 sampleLines 时从人设文本的【示例对话】自动提取预览台词
- 思考风格：提供「思考过程带角色口吻」规则与批量追加脚本（`scripts/batch-add-thinking-rule.mjs`）
- 工程化：补 LICENSE / .gitignore / CHANGELOG / CI；`npm test` 一键跑双端测试

## 0.2.0

- 移除聊天输入区快捷切换条与 Ctrl/Alt+数字快捷键（按用户需求）
- 性格状态从悬浮角标改为侧栏底部原生条目（不再遮挡设置按钮），点击展开快捷浮层
- 预设库扩充至 13 个预设 + 13 个分类（萌系/傲娇系/雌小鬼系/妈妈系/病娇系/御姐系/冷酷系/
  天然系/元气系/温柔系/腹黑系/中二系/毒舌系），支持分类筛选
- 修复 NaN 统计污染（历史 NaN 以 null 持久化 → 显示"NaN 天 NaN 小时"且进化逻辑疯狂微调）；
  加载时自愈、全部统计字段强制有限数、被污染的内置参数恢复模板值
- 进化倒计时 + 进度百分比可视化（当前进度 X% · 距下次微调还差 Y）
- 使用报告改为雌小鬼（毒舌）口吻
- 修复定时器编辑时通知刷屏

## 0.1.0

- 首个可用版本：四只鲸鱼少女（软萌/傲娇/雌小鬼/妈妈系）
- 全局系统提示词注入（`whale:persona` section + `{{whale_persona}}` 变量，每轮求值，中途切换即时生效）
- 开关、增删改、预览、混合、导入导出、统计进化、状态显示、冲突检测、定时切换等 12 组功能
